# ========================================
# Backoffice Frontend - 환경변수 설정
# 이 파일을 .env.local 로 복사 후 값을 수정하세요
# cp .env.example .env.local
# ========================================

# 백엔드 API URL (base, /api/admin 경로는 코드에서 추가)
NEXT_PUBLIC_API_URL=http://localhost:8080
